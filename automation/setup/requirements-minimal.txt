# Insightron v4.1.1 - Minimal Requirements
# Use this if the main requirements.txt fails
# Optimized for compatibility and faster installation

# Core Whisper AI (latest stable)
faster-whisper>=1.0.0
tokenizers>=0.19.1

# Essential audio processing (minimal versions)
librosa>=0.10.0
soundfile>=0.12.0
noisereduce>=3.0.0
pyloudnorm>=0.1.1

# Progress tracking
tqdm>=4.60.0

# Audio format support
pydub>=0.25.0

# Core scientific computing (minimal versions)
numpy>=1.21.0
scipy>=1.7.0

# Enhanced error handling
colorama>=0.4.6

# Basic LLM support
transformers>=4.40.0
torch>=2.0.0
openai>=1.0.0

# Speaker diarization (optional; requires HF token + heavier deps)
pyannote.audio>=3.1.1
torchaudio>=2.0.0
huggingface_hub>=0.20.0
