# Insightron v4.1.1 - Python 3.14-ready Requirements
# Optimized for performance, reliability, and cross-platform compatibility.

# Core Whisper AI (faster-whisper with CTranslate2)
# v1.2.1+ uses importlib.metadata and current CTranslate2 releases.
# Includes batched inference for 4x faster batch processing
faster-whisper>=1.2.1,<2.0.0

# Explicitly list tokenizers to ensure we get a compatible version
# or trigger the Rust build requirement clearly
# Note: May require Rust for building from source on some systems
tokenizers>=0.23.1,<1.0.0

# Essential audio processing (optimized versions for stability)
# librosa requires numpy, scipy, and soundfile
librosa>=0.11.0,<1.0.0
soundfile>=0.13.1,<1.0.0
noisereduce>=3.0.3,<4.0.0
pyloudnorm>=0.2.0,<1.0.0

# Progress tracking and utilities
tqdm>=4.67.0

# Audio format support (enhanced compatibility)
pydub>=0.25.1

# Core scientific computing (Windows-compatible versions)
# Python 3.14 requires NumPy 2.x and newer SciPy wheels.
numpy>=2.2.0,<3.0.0; python_version < "3.14"
numpy>=2.3.0,<3.0.0; python_version >= "3.14"
scipy>=1.15.0,<2.0.0; python_version < "3.14"
scipy>=1.16.0,<2.0.0; python_version >= "3.14"

# Optional: For advanced audio analysis and visualization
matplotlib>=3.10.0,<4.0.0

# Development and testing (optional)
pytest>=8.4.0,<9.0.0

# Build and packaging tools. Torch 2.12 supports setuptools <82.
setuptools>=80.0.0,<82.0.0

# Enhanced error handling and logging
colorama>=0.4.6

# Modern UI Framework
# customtkinter requires tkinter (usually included with Python)
customtkinter>=5.2.2,<6.0.0
packaging>=25.0

# Realtime Audio Capture
sounddevice>=0.5.5,<1.0.0

# Memory Monitoring
psutil>=7.0.0,<8.0.0; python_version >= "3.14"
psutil>=5.9.8,<8.0.0; python_version < "3.14"

# Configuration file parsing
PyYAML>=6.0.3,<7.0

# LLM & Multi-Pass Dependencies
# Required for contextual restoration pass
transformers>=5.0.0,<6.0.0; python_version >= "3.14"
transformers>=4.57.0,<6.0.0; python_version < "3.14"
torch>=2.12.0,<3.0.0; python_version >= "3.14"
torch>=2.9.0,<3.0.0; python_version < "3.14"
accelerate>=1.11.0,<2.0.0
bitsandbytes>=0.43.0; platform_system != "Windows"
openai>=2.0.0,<3.0.0

# Speaker diarization (optional, single-phase enrichment)
pyannote.audio>=4.0.0,<5.0.0; python_version >= "3.14"
pyannote.audio>=3.4.0,<5.0.0; python_version < "3.14"
torchaudio>=2.11.0,<3.0.0; python_version >= "3.14"
torchaudio>=2.9.0,<3.0.0; python_version < "3.14"
huggingface_hub>=1.0.0,<2.0.0
